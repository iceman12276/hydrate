# Flat node_modules so Metro/Expo and Tauri resolve workspace deps without
# pnpm symlink surprises.
node-linker=hoisted

# Supply-chain hardening: wait 7 days before resolving newly published versions.
min-release-age = 7
