# Supply-chain hardening (enforced): wait 7 days before installing newly
# published versions, block exotic transitive sources, never downgrade trust.
minimumReleaseAge: 10080
blockExoticSubdeps: true
trustPolicy: no-downgrade

packages:
  - apps/*
  - packages/*
